Re-extract the account-scoped families. The reused extracts are stale: April transactions reference 2,803,758 accounts but only 2,022,076 were extracted — 943,439 missing in April alone, which is why the build dropped 4.87M txn_for_account edges.

The cause: those files came from the old trade_dt extraction with the 5,746 cohort. Under proc_dt with 5,455 advisors, scoped_acct resolves to a different account set.

1 · Move the stale files aside:

powershell
Move-Item data\real\_raw\raw_account_b*.csv       data\real\_raw_old_trade_dt\
Move-Item data\real\_raw\raw_acct_eci_rel_b*.csv  data\real\_raw_old_trade_dt\
Move-Item data\real\_raw\raw_acct_eci_map_b*.csv  data\real\_raw_old_trade_dt\
Move-Item data\real\_raw\raw_balance_*.csv        data\real\_raw_old_trade_dt\

2 · Remove their checkpoint entries — the 12 bucket chunks and 3 balance chunks — keeping everything else, especially raw_adv_flows and all raw_txn_*.

3 · Dry-run first. Only those 15 chunks should be pending. If any raw_txn_* shows pending, stop and report — the checkpoint edit went wrong and you'd re-extract 84 chunks unnecessarily.

4 · Extract, then rebuild with --max-memory-mb 8192.

Report the new txn_for_account and am_for_account dropped counts. They should fall to near zero.
-----------------------------------------------------------------------------------------------------------------------
Start the graph load. The build is complete and verified: 40,047,855 vertex rows and 104,273,975 edge rows across 49 files.

powershell
uv run python scripts\load_real_data.py --data-dir data\real --max-parallel 3

Expect about 3 hours. Two phases, enforced: all 18 vertex entities, then all 31 edge entities. A phase-2 entity refusing to start while phase 1 is incomplete is correct behaviour, not an error — rerun to finish phase 1 first.

If interrupted, rerun the identical command. Checkpoints skip completed entities. Never pass --fresh — it discards all completed work.

Report at the end of phase 1 (elapsed time and rows loaded), then at the end of phase 2. Do not stop to ask in between.

When the load finishes, run reconciliation and paste the complete output:

powershell
uv run python scripts\reconcile_load.py --raw data\real\_raw --data-dir data\real

Two identical failures = stop and report. Never a third attempt. Never estimate a number.
--------------------------------------------------------------------------------------
Read docs/COPILOT_FIX_CONNECTIVITY_AND_LOAD.md in full before running anything.

The load failed completely — all 18 vertex entities were served by the local fallback store instead of TigerGraph. The root cause is already identified in Part A: the wrong environment variable family is set. app/graph/tiered_client.py reads TG_HOST first and only falls back to TIGERGRAPH_HOST, and the TG_* family is the only one that carries ports and credentials. Do not re-diagnose; verify and fix.

Do not tune memory. The 24.6 GB was rows buffering for a destination that never accepted them — a symptom, not the cause.

Do not start the full load until A3 and A4 pass: the client class must be the TigerGraph client, and loading month alone must show 3 rows in GSQL. A 144M-row load on an unproven connection is what the last attempt cost.

Also fix the blank advisor_sid on row 1 of phx_dm_pce_advisor.csv (Part C) — it should be the literal __UNATTRIBUTED__. Fix the value; never delete the row.

Run the load from a standalone PowerShell window, not the IntelliJ terminal — the IDE hung on this output volume once already.
---------------------------------------------------------------------------------------
Read docs/COPILOT_DIAGNOSE_LOAD_SPEED.md in full and work through all six sections.

This is analysis only. Do not change any code. Do not stop or restart the load — it can keep running while you investigate.

The problem: measure_ingest_rate.py measured 7,706 rows/sec, but the real load is running at roughly 133 rows/sec — 58× slower. At that rate 144M rows would take 300 hours instead of 3.

Three things are already ruled out and must not be re-checked: batch_size is 5000 in the manifest, _flush_writes() makes one bulk adapter call per flush rather than one per row, and connectivity is genuinely real (tier 2 pytigergraph, no fallback).

Section 5 is the most important — determine exactly how measure_ingest_rate.py differed from the real load path, especially whether it bypassed IngestionService and its per-row hash checkpointing. If it did, the measurement was honest but unrepresentative.

Report using the exact template at the end of the document, and finish with one sentence naming the bottleneck.

Never estimate a number — every figure comes from a command that ran. If a file or symbol is not where expected, report it rather than searching elsewhere.

Two identical failures = stop and report. Never a third attempt. Never estimate a number.