The OR is still in the template — the fix was never applied to flows, only to rpg. And there's a second index-killer I hadn't spotted: f.bus_dt::date in the WHERE clause prevents index use on the date column too.

Here's the corrected query. Run one month at a time.

sql
SET statement_timeout = '900s';

SELECT r.standard_id                             AS advisor_sid,
       to_char(f.bus_dt,'YYYYMM')                AS month_id,
       f.cwm_flow_comp_prod_cd                   AS flow_product_cd,
       COALESCE(f.cwm_flow_comp_prod_desc_tx,'') AS flow_product_desc,
       COALESCE(f.cwm_comp_group_type,'')        AS comp_group_type,
       sum(COALESCE(f.total_inflows_am,0))                  AS total_inflows,
       sum(COALESCE(f.total_outflows_am,0))                 AS total_outflows,
       sum(COALESCE(f.total_net_financial_flows,0))         AS total_net_flows,
       sum(COALESCE(f.total_cwm_comp_credited_flows_am,0))  AS credited_flows,
       max(COALESCE(f.other_attributes->>'departed_advisor_sid',''))                 AS departed_advisor_sid,
       sum(COALESCE((f.other_attributes->>'departed_advisor_excl_am')::numeric,0))   AS departed_advisor_excl_am,
       sum(COALESCE((f.other_attributes->>'lob_trfr_excl_am')::numeric,0))           AS lob_trfr_excl_am,
       sum(COALESCE((f.other_attributes->>'oi_pa_referral_cap_adj_am')::numeric,0))  AS oi_pa_referral_cap_adj_am,
       sum(COALESCE((f.other_attributes->>'large_flow_cap_adj_am')::numeric,0))      AS large_flow_cap_adj_am,
       sum(COALESCE((f.other_attributes->>'forced_closure_excl_am')::numeric,0))     AS forced_closure_excl_am
FROM   pcr.fpic_daily_adv_flows_tb_pm f
JOIN   pcr.fpic_prm_rr_tb r
       ON r.standard_id = f.rep_wrkr_sid
WHERE  f.bus_dt >= DATE '2026-04-01'
  AND  f.bus_dt <  DATE '2026-05-01'
GROUP  BY 1, 2, 3, 4, 5;

Three changes from what's failing:

OR removed — single indexed equality. Your diagnostic showed both columns 100% populated, so rep_wrkr_sid alone is correct.
f.bus_dt::date → f.bus_dt — casting a column in WHERE disables the index. This alone could be the whole timeout.
Cohort filter dropped — all 5,746 advisors are in scope, so IN (SELECT ... FROM cohort_adv) filters nothing while costing a subquery.

Run it for April, then change the dates for May (2026-05-01 / 2026-06-01) and June (2026-06-01 / 2026-07-01).

Save as raw_adv_flows_202604.csv and so on, in data/real/_raw/. Expect roughly 55k rows per month, ~167k total.

If April still times out, run EXPLAIN on it and send me the plan


-----------------------------------
copilot text


Good progress. Continue the transaction chunks to completion and report at 40, 60 and 87.

Once transactions are done, retry flows as-is for one month — the merge join may now complete within 900s, since the earlier failures were the nested loop rather than the volume. If April still times out, split flows by month and advisor batch (three batches of ~2,000 advisors per month, nine sub-chunks) and report.

Do not retry flows while transactions are still running.
Continue to completion. Report at 40, 60 and 87 chunks with completed count, elapsed time and any failure. Do not stop to ask between milestones. Refresh the token and rerun the same command whenever it expires.

Once all 87 transaction chunks are done, retry flows as-is for one month — the merge join may now complete within 900s. Report the result either way.

---------------------------round 5
Fix the rpg cast. rpg is an integer column, so COALESCE(d.rpg,'') cannot coerce an empty string into it. It must be COALESCE(d.rpg::text,'') AS rpg — the graph column is a STRING.

This was fixed once before and regressed, so find where the executing SQL actually comes from:

powershell
Select-String -Path scripts\generate_extraction_sql.py -Pattern "COALESCE\(d\.rpg"
Get-ChildItem docs\data\extraction\raw_revenue_transaction.sql -ErrorAction SilentlyContinue
Get-ChildItem -Recurse -Filter "__pycache__" -Directory | Remove-Item -Recurse -Force

Delete any stale generated .sql file and clear __pycache__. Then prove the fix is live in a fresh process before running any chunk:

powershell
uv run python -c "
from scripts.generate_extraction_sql import templates
import re
s = templates()['raw_revenue_transaction.sql']
print([l.strip() for l in s.splitlines() if 'rpg' in l][:3])
"

The rpg line must show ::text. Do not run a chunk until that output confirms it.

While you are there, check every other COALESCE(col,'') in the transaction template against the column's actual type in information_schema.columns, and report any others that are non-text. A second type error would cost another failed chunk.

Then run the extraction and report the first 5 chunks with wall time and row counts.