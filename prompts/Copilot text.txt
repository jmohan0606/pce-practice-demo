Stop the extraction loop. You are retrying a failing chunk rather than making progress.

Report, without running any extraction:

cat data/real/_raw/extract_checkpoint.json — how many chunks completed, and the last one
ls -la data/real/_raw/*.csv | wc -l
The exact error from the most recent failed attempt, in full
Was the rpg::text cast actually applied? Show the line from scripts/generate_extraction_sql.py

Do not retry anything until you have reported these four things.