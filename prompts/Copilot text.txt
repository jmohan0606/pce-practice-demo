Run the PCL login from CONNECTION_DETAILS.md section 1, re-export PCE_PG_DSN with the fresh token, then rerun the identical extraction command without --skip.

The 21 completed chunks are checkpointed and will be skipped. Expect the token to expire again during the remaining 87 — refresh and rerun each time. Do not pass --restart.

Report every 20 chunks: completed count, elapsed time for those chunks, and any failure.