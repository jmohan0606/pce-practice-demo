"""Round A1 task 6 — export service (Subagent C).

One provider per section returns a normalized table payload; four renderers
(pdf/pptx/xlsx/csv) consume that payload. Adding a section is one provider,
not four renderers.
"""
